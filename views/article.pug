extends layout

block content  
    .uk-section
        .uk-container
            ul.uk-breadcrumb  
                li: a(href="/") Home
                li: a(href="/blog/collections") Blog
                li= articleTitle
            .uk-article 
                h1.uk-article-title= articleTitle 
                img.uk-margin-medium-bottom(src=`https://radiant-atoll-86561.herokuapp.com${articleHero.url}` alt=`${articleHeroAltText}` )
                .uk-article-meta
                    img.uk-border-circle(width="60" height="60"  src=`https://radiant-atoll-86561.herokuapp.com${author.authorImage.url}` )
                    p= `Written By ${author.authorName} on ${Date.substr(0,10)}` 
                p.uk-text-lead= articleDescription 
                p#content(style="white-space:pre-wrap")= articleContents

block scripts
    script(src="https://cdn.rawgit.com/showdownjs/showdown/1.9.0/dist/showdown.min.js")
    script. 
        var conv = new showdown.Converter();
        var txt = document.getElementById('content').innerHTML;
        document.getElementById('content').innerHTML = conv.makeHtml(txt);